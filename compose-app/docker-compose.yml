version: '3'
services:
  web:
   image: nginx:latest
   ports:
     - "8082:80"
   volumes:
     - ./html:/usr/share/nginx/html
  db:
   image: postgres:15
   environment:
     POSTGRES_PASSWORD: mypassword
     POSTGRES_DB: mydb
